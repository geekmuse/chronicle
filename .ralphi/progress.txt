## Codebase Patterns
- `git::RepoManager`: wraps `git2::Repository`; `init_or_open(path, remote_url)` → open if exists (ErrorCode::NotFound triggers init), set remote if provided; `ensure_working_tree()` creates pi/sessions/ + claude/projects/ (both with .gitkeep) + .chronicle/; `ensure_manifest()` writes default if absent; all public methods are idempotent
- `git::GitError`: thiserror enum — `Git2(git2::Error)`, `Io { path, source }`, `Manifest(String)`; private `GitError::io(path, source)` constructor reduces boilerplate
- `git::Manifest` / `git::MachineEntry`: serde structs for `.chronicle/manifest.json`; `version: u32` (always 1), `machines: HashMap<String, MachineEntry>`; `last_sync` is `Option<DateTime<Utc>>` with `skip_serializing_if = "Option::is_none"`
- `open_or_init(path)`: module-private helper; `Repository::open` → if `ErrorCode::NotFound` → `create_dir_all` + `Repository::init`; other errors propagated
- `set_remote(name, url)`: private method; `find_remote` → if NotFound add, else update URL via `remote_set_url`
- `.gitkeep` files placed in `pi/sessions/` and `claude/projects/` so git tracks empty dirs; `.chronicle/` left without .gitkeep (manifest.json serves that purpose)
- `serde_json::to_string_pretty` for manifest writes (human-readable JSON)
- `scan::StateCache`: `files: HashMap<String, FileState>`; `load(path)` (missing → empty), `save(path)` (atomic temp+rename), `default_path()` (`~/.local/share/chronicle/state.json`); key is canonical repo path in production, absolute local path in unit tests
- `scan::FileState`: `local_mtime: DateTime<Utc>`, `local_size: u64`, `last_synced_size: u64`, `local_path: PathBuf`; serialized with `skip_serializing_if` is NOT needed here (all fields required)
- `scan::scan_dir(dir, cache, follow_symlinks)` → `Result<Vec<ScanEntry>, ScanError>`; checks symlink on `dir` via `fs::symlink_metadata`; walks via `collect_jsonl` (skips inner symlinks); classifies via `classify(path, mtime, size, cache)` using abs-path key
- `scan::ChangeKind`: `New` (key absent), `Modified` (key present, mtime or size differs), `Unchanged` (both match)
- `scan::ScanError`: `SymlinkRefused(PathBuf)` | `Io { path, source }`
- `DateTime::<Utc>::from(system_time)` — idiomatic `SystemTime → DateTime<Utc>` conversion; `Utc.timestamp_opt(secs, nsecs)` needs `use chrono::TimeZone;` in the test mod
- `std::io::Error::other(e)` — prefer over `std::io::Error::new(ErrorKind::Other, e)` (clippy::io_other_error fires on the latter)
- `collect_jsonl` uses `DirEntry::file_type()` (lstat, no symlink follow) and skips `ft.is_symlink()` entries — only the top-level `dir` symlink is policy-checked

- `merge::entry`: `EntryKey` enum (`Header` | `Entry{entry_type,id}`); `parse_entry(raw)` returns `Option<ParsedEntry>`; `extract_timestamp(value)` checks `timestamp`, `created_at`, `createdAt` fields
- `merge::set_union::merge_jsonl(remote, remote_path, local, local_path, reporter)` → `MergeOutput { content, malformed, conflicts }`; remote entries populate first (win on collision); prefix verification (§5.4) detects byte-inequivalent common entries
- `ConflictReporter` trait in `merge::set_union` — `report_prefix_mismatch(file, entry_key, local_raw, remote_raw)`; `NullReporter` is the no-op impl; real ring buffer impl comes in US-008
- `PrefixConflict { file, entry_key, local_raw, remote_raw }` — one record per divergent common entry; included in `MergeOutput::conflicts`
- Prefix verification loop: iterate local_entries; if key in remote map AND raws differ → warn + reporter.report + push conflict; remote version stays in map (skip local); if key not in remote map → insert local
- `MalformedLine { path, line_number, snippet }` — 1-based line, up to 80-char snippet; emitted via `tracing::warn!` and returned in `MergeOutput::malformed`
- Stable sort tie-break: `Source::Remote = 0`, `Source::Local = 1`, then `original_index` — guarantees deterministic output for equal timestamps
- `Path::new("x")` is NOT const-stable in current Rust edition; use `fn helper() -> &'static Path { Path::new("x") }` in test constants
- `#![allow(dead_code)]` needed on both `entry.rs` and `set_union.rs` until US-007/US-015 wire in the merge module
- `canonicalize_line(line, level)` / `decanonicalize_line(line)` on `TokenRegistry` — content-level L2/L3 dispatch in `src/canon/levels.rs`; returns `Result<String, ChronicleError>`
- `try_canonicalize_path(s)` — L2: returns `Some(canonical)` only if `s == $HOME` or `s.starts_with($HOME/)` (strict prefix, boundary `/` or EOS); custom tokens applied prefix-only with `break`
- `try_canonicalize_text(s)` — L3: `replace_in_text` scans globally; custom tokens also applied globally (no `break`)
- `try_decanonicalize_text(s)` — reverses both L2 and L3; always applied to ALL string values in de-canon (safe because token pattern unlikely to appear naturally)
- `replace_in_text(text, from, to)` — path-boundary replacement: `from` must be followed by `/` or EOS; prevents partial matches (e.g., `bradmatic2` won't match `bradmatic`); handles multiple occurrences in a single pass
- `canonical_token_value(path, home, token)` — converts a custom token's raw path value to its canonical storage form (`{{SYNC_HOME}}/Dev` for `/Users/bradmatic/Dev`)
- `fields::WHITELISTED` — 7 entries per spec §4.3; use `apply_to_whitelisted(obj, fn)` for L2 and `apply_to_all_strings(value, fn)` for L3
- `L3_WARNING` const in `levels.rs` — must be emitted by CLI commands (US-013, US-014, US-017) at startup when `level >= 3`
- proptest in `levels.rs` tests: import `use proptest::prelude::*;` inside the `mod tests` block (not at crate root)


- `TokenRegistry` in `src/canon/mod.rs` — built via `from_config(&CanonicalizationConfig, &Path)`; exposes `canonicalize_pi_dir`, `canonicalize_claude_dir`, `decanonicalize_pi_dir`, `decanonicalize_claude_dir`
- Canonicalization order: SYNC_HOME first, custom tokens after (applied to already-canonical form using canonical-encoded token values); de-canon reverses the order
- `boundary_match(s, prefix)` helper: character after prefix in `s` must be `-` or end-of-string; prevents partial matches (e.g., `/Users/bradmatic2` ≠ `/Users/bradmatic`)
- Custom tokens sorted by descending value-path length at construction time so most-specific prefix wins; only the first match applies (prefix-only replacement, so `break` after first hit is correct)
- Canonical encoded form of a custom token value: encode the raw path with agent encoding, then apply SYNC_HOME prefix replacement to that encoded string — this is the pattern matched against the already-canonicalized directory inner string
- `pi_encode_inner` / `claude_encode_inner` are module-private helpers; prefer `canonicalize_*_dir` / `decanonicalize_*_dir` in callers
- `Agent` trait in `src/agents/mod.rs` — `session_dir`, `encode_dir`, `decode_dir`; `PiAgent` and `ClaudeAgent` are unit structs implementing it
- Pi directory encoding: strip leading `/`, replace `/` with `-`, wrap with `--`; decoding is lossless only for components without `-`
- Claude directory encoding: strip leading `/`, replace `/` and `.` with `-`, prefix with `-`; decoding is lossless only for components without `-` or `.`
- Claude `decode_dir` explicitly rejects `--`-prefixed (Pi-format) names to catch cross-agent mistakes
- `cargo fmt` must be run (not just `--check`) after `Write` tool outputs code; rustfmt reformats `let x = name.strip_prefix(...)` differently from `let x = name\n    .strip_prefix(...)`
- Config module uses `pub(crate)` helpers (`apply_env_overrides`, `apply_cli_overrides`, `config_path_with_xdg_home`) to test precedence logic without mutating `std::env` (which is unsafe in parallel tests)
- `write_all(b"raw bytes")` in tests instead of `writeln!(f, r#"..."#)` avoids `{{` / `}}` being consumed as escaped format braces by the `writeln!` macro
- Separate `PiAgentConfig` / `ClaudeAgentConfig` structs needed because a shared `AgentConfig` can't have per-agent `session_dir` defaults with serde; distinct types each have their own `#[serde(default = "...")]` functions
- `#[derive(Default)]` on `Config` works after removing manual `impl Default` that just called `field::default()` — clippy::derivable_impls catches these
- `HistoryMode` enum uses `#[serde(rename_all = "lowercase")]` + `#[default]` on `Partial` variant for clean TOML deserialization
- `cargo check` is fast after first compile; `cargo test` recompiles a separate test binary
- ICU unicode crates (icu_normalizer_data, icu_properties_data) come in via git2→url→idna→icu_normalizer; first compile takes ~10min, cached afterwards
- git2 pinned to 0.18 (not 0.19/0.20) to avoid icu_normalizer_data in the dependency chain
- Dead-code for foundational error types: use `#![allow(dead_code)]` at the module level with a comment pointing to the stories that will wire it in
- All cli handler stubs live in `src/cli/mod.rs`; main.rs only owns the clap struct and match dispatch
---

## 2026-03-29 - US-002
Session: iter-b80f0cc5 / loop-26ce4e61 iteration 2
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented full configuration system per §8 of the spec
- Created src/config/schema.rs: serde structs for all 6 config sections
  ([general], [notifications], [storage], [canonicalization], [agents.pi],
  [agents.claude], [sync]); each field has a #[serde(default)] with the §8.2
  value; HistoryMode enum with lowercase serde rename; PiAgentConfig and
  ClaudeAgentConfig are separate types (different session_dir defaults)
- Created src/config/machine_name.rs: adjective-animal generator with static
  ADJECTIVES (47) and ANIMALS (90) slices; uses rand::seq::SliceRandom;
  4,200+ possible combinations; 4 unit tests
- Rewrote src/config/mod.rs: load() function implementing CLI > env > file >
  defaults precedence chain; apply_env_overrides() and apply_cli_overrides()
  kept pub(crate) for test isolation; config_path_with_xdg_home() is
  pub(crate) to test XDG logic without mutating std::env; expand_path() for
  tilde expansion; 17 unit tests covering all acceptance criteria
- Fixed cargo fmt formatting issues after initial write
- Fixed writeln! brace escaping bug in test: `{{SYNC_PROJECTS}}` in a format
  string becomes `{SYNC_PROJECTS}` — fixed by using write_all(b"...") instead
- Fixed clippy::derivable_impls: `impl Default for Config` was manually
  forwarding to sub-defaults; replaced with #[derive(Default)]
- All quality checks pass: cargo fmt --check, cargo clippy -D warnings,
  cargo test (21 pass), cargo deny check
- **Learnings for future iterations:**
  - writeln!(f, r#"..."#) in tests: `{{` / `}}` are STILL consumed by the
    format macro even in raw strings — use f.write_all(b"...") for literal
    byte content with no format interpolation
  - std::env::set_var is `unsafe` in Rust 1.81+; avoid in tests by extracting
    the XDG logic into a testable helper that accepts the value as a parameter
  - clippy::derivable_impls fires when you manually implement Default by just
    calling each field's default(); use #[derive(Default)] instead
  - Separate agent config structs (PiAgentConfig, ClaudeAgentConfig) needed
    when two TOML subsections share the same struct shape but have different
    field defaults; serde can't parameterise default functions per-instance
---

## 2026-03-29 - US-003
Session: iter-f0ddf726 / loop-26ce4e61 iteration 3
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `Agent` trait and `PiAgent`/`ClaudeAgent` structs in `src/agents/mod.rs`
- Pi: encode wraps path components (/ → -) with `--` delimiters; decode strips delimiters and restores /
- Claude: encode replaces both / and . with `-`, prefixes with single `-`; decode strips prefix and restores /
- 13 unit tests: simple encode/decode, session_dir, round-trips (simple paths), dot-in-path encode,
  error paths (missing wrapper, Pi format rejected by Claude), cross-agent differentiation
- `cargo fmt` reformatted the `decode_dir` chain in `ClaudeAgent` — always run `cargo fmt` (not `--check`) after writing code
- `#![allow(dead_code)]` carried over at module level; will be removed when US-004/US-013/US-014 wire it in
- Files changed: `src/agents/mod.rs`
- **Learnings for future iterations:**
  - Pi/Claude directory encoding is inherently lossy for path components containing `-` (Pi) or `-`/`.` (Claude); round-trip tests restrict to simple component names
  - `str::strip_prefix("--").and_then(|s| s.strip_suffix("--"))` is idiomatic for validating both ends of a wrapper in one chain
  - Add a `starts_with("--")` guard to Claude `decode_dir` before `strip_prefix('-')` — otherwise Pi-encoded names silently decode to garbage
---

## 2026-03-29 - US-004
Session: iter-f1aacb91 / loop-26ce4e61 iteration 4
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `TokenRegistry` in `src/canon/mod.rs`:
  - `from_config(&CanonicalizationConfig, &Path)` builds the registry; custom tokens sorted longest-value-first
  - `canonicalize_pi_dir` / `canonicalize_claude_dir` — L1 directory name canonicalization
  - `decanonicalize_pi_dir` / `decanonicalize_claude_dir` — L1 directory name de-canonicalization
  - `apply_tokens_to_inner` / `revert_tokens_from_inner` — internal helpers
- `boundary_match` helper ensures `/Users/bradmatic2` does NOT match when home is `/Users/bradmatic`
- Custom token application works in canonical space: raw token value is encoded via agent rules then SYNC_HOME'd to produce a canonical-encoded pattern that matches against the already-SYNC_HOME'd inner string
- 25 unit tests covering all acceptance criteria
- Files changed: `src/canon/mod.rs`
- **Learnings for future iterations:**
  - Custom tokens for L1 must be applied in their *canonical encoded* form — encode the raw path value with the agent's encoding rules, then apply SYNC_HOME to produce the pattern to search for in the canonical directory inner string
  - `boundary_match` uses byte-offset indexing (`s[prefix.len()..]`) — safe here because all encoded path chars and token names are ASCII; document this assumption if Unicode home paths ever become a concern
  - Only prefix replacement is needed for L1 (directory names are a single encoded path); `break` after first custom token match is correct
  - De-canon order (custom tokens first, then SYNC_HOME) ensures that a token like `{{SYNC_PROJECTS}} = {{SYNC_HOME}}/Dev` gets fully expanded back to the local path
---

## 2026-03-29 - US-006
Session: iter-e93c4a03 / loop-26ce4e61 iteration 6
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented JSONL entry parsing (§5.1) and grow-only set merge (§5.2 steps 1/3/4/5)
- Created src/merge/entry.rs: EntryKey enum, ParsedEntry struct, parse_entry(), extract_timestamp(); 15 unit tests
- Created src/merge/set_union.rs: merge_jsonl(), MalformedLine, MergeOutput, internal TaggedEntry+Source for stable sort; 22 unit tests covering all §5.3 scenarios, malformed line handling, empty files, one-sided merge, duplicate key (remote wins), stable sort tie-break
- Updated src/merge/mod.rs: pub mod declarations for entry and set_union
- 147 tests total (37 new); all passing; cargo fmt, clippy -D warnings, cargo deny all pass
- **Learnings for future iterations:**
  - `Path::new("x")` is NOT const-stable in the current Rust edition; can't use it in `const` — use a `fn` returning `&'static Path` instead
  - HashMap insertion order is non-deterministic; always collect + sort rather than relying on iteration order from a HashMap
  - Prefix verification (§5.2 step 2) and its logging are deliberately deferred to US-007 — merge_jsonl silently uses remote-wins without warning
  - `uuid` used as fallback id field to accommodate Claude session format; documented in entry.rs comment
  - sed substitution of test variable names (e.g., `let local = "..."`) can collide with helper function names — rename helpers to `local_path()` / `remote_path()` to avoid shadowing
---

## 2026-03-29 - US-007
Session: iter-2976d977 / loop-26ce4e61 iteration 7
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented prefix verification (§5.4) and `ConflictReporter` interface in `src/merge/set_union.rs`
- Added `ConflictReporter` trait with `report_prefix_mismatch(file, entry_key, local_raw, remote_raw)`
- Added `NullReporter` (no-op impl, `#[derive(Debug, Default)]`) for use until US-008 wires in the real ring buffer
- Added `PrefixConflict { file, entry_key, local_raw, remote_raw }` record type
- Extended `MergeOutput` with `conflicts: Vec<PrefixConflict>` field
- Updated `merge_jsonl` signature: added `reporter: &dyn ConflictReporter` parameter
- Prefix verification loop: for each local entry with a key present in remote map, compare `raw` byte-by-byte; mismatch → `tracing::warn!` + `reporter.report_prefix_mismatch()` + push to `conflicts`; remote version stays; local skipped
- Updated all 22 existing tests to pass `&NullReporter`; added 6 new US-007 tests
- Moved `RefCell` import into `mod tests` block to avoid top-level unused-import warning
- Files changed: `src/merge/set_union.rs`
- 153 tests total; all passing; cargo fmt, clippy -D warnings, cargo deny all pass
- **Learnings for future iterations:**
  - `RefCell` used in test-local `RecordingReporter` — import it inside `mod tests`, not at file top-level, to avoid `unused-imports` lint
  - `tracing::warn!` with `%` prefix uses `Display` (works for `path.display()`) and `?` prefix uses `Debug` (works for `EntryKey`); `.chars().take(80).collect::<String>()` inline in the macro arg is fine
  - `ConflictReporter` trait methods return `()` so no `#[must_use]` needed on them; but `merge_jsonl` has `#[must_use]` — test calls that intentionally discard the result need `let _ = ...`
  - Doc comments with long continuation lines (`///   ...`) must not over-indent; clippy::doc_overindented_list_items fires if continuation indentation > 2 spaces from `///`
---

## PRD Run Context
PRD Branch: ralph/initial-delivery
Started: 2026-03-29T02:25:06.047Z
---

## 2026-03-29 - US-001
Session: iter-cb54e7b0 / loop-26ce4e61 iteration 1
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented full Rust project scaffolding from scratch
- Created src/ with 9 module directories: cli, config, canon, merge, git, agents, scheduler, errors, scan
- main.rs: clap derive CLI with all 9 commands (init, import, sync, push, pull, status, errors, config, schedule); schedule has install/uninstall/status subcommands; all handlers print "not implemented" and return Ok
- errors/mod.rs: ChronicleError with thiserror, 7 categories per §11.1, category() method, unit tests; #![allow(dead_code)] module-level until US-007/US-008 wire it in
- Downgraded git2 0.19→0.18: icu_normalizer_data transitive dep (via url→idna v1.1→icu_normalizer) causes 10+ min first compile on 0.19; 0.18 also pulls it but it's already cached after first build
- All quality checks pass: cargo fmt --check, cargo clippy -D warnings, cargo test (2 tests), cargo deny check
- **Learnings for future iterations:**
  - First `cargo test` takes much longer than `cargo check` (separate test binary, includes proptest/tempfile/rusty-fork)
  - `pub` items in a binary crate DO get dead_code warnings if never referenced from main; use `#![allow(dead_code)]` at module level for foundational scaffolding
  - git commit hooks run `ralphi check` which calls all configured quality commands — keep those passing before committing
---
## 2026-03-29 - US-005
Session: iter-bc406038 / loop-26ce4e61 iteration 5
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented L2 structured field canonicalization, L3 freeform canonicalization,
  and full de-canonicalization per spec §4.2–4.3, §4.7
- Created src/canon/fields.rs: WHITELISTED constant (7 field paths from §4.3),
  apply_to_whitelisted (only touches specified JSON field paths), apply_to_all_strings
  (recursive tree walker for L3 full-string scan); 8 unit tests
- Created src/canon/levels.rs: canonicalize_line (level<2 passthrough, level=2 uses
  whitelisted, level>=3 uses all-strings), decanonicalize_line (global reversal to
  all string values), L3_WARNING const; 27 unit tests + 3 proptest round-trip properties
- Updated src/canon/mod.rs: added try_canonicalize_path (L2 prefix match), 
  try_canonicalize_text (L3 global scan), try_decanonicalize_text (custom tokens
  then SYNC_HOME reversal), replace_in_text helper (/-or-EOS boundary),
  canonical_token_value helper; 24 new unit tests
- Files changed: src/canon/mod.rs, src/canon/fields.rs (new), src/canon/levels.rs (new)
- 110 tests total, all passing; cargo fmt, clippy -D warnings, cargo deny all pass
- **Learnings for future iterations:**
  - replace_in_text: boundary is "/" or EOS — prevents bradmatic2 matching bradmatic;
    for L3 custom token application this means exact paths without a subdir might
    not have the custom token applied, but SYNC_HOME still applies and round-trip holds
  - For L2, try_canonicalize_path does prefix-only matching and breaks after first
    custom token hit; for L3, try_canonicalize_text applies ALL custom tokens globally
    (no break) so multiple occurrences in freeform text all get canonicalized
  - decanonicalize_line applies to ALL string values regardless of level — safe because
    the token pattern {{...}} doesn't appear naturally in session content
  - proptest strategies go inside `mod tests` block, import `use proptest::prelude::*;`
    inside the mod (not at the file top level)
  - L3_WARNING needs to be emitted by CLI sync/import commands (US-013/US-014/US-017)
    when config.canonicalization.level >= 3
---

## 2026-03-29 - US-008
Session: iter-9a1e110a / loop-26ce4e61 iteration 8
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented 30-entry error ring buffer (§11.1) in `src/errors/ring_buffer.rs`
- `ErrorEntry` struct: timestamp (chrono::DateTime<Utc> with serde), severity (Severity enum: error|warning|info, lowercase), category (String), file (Option<String>, skipped when None), message (String), detail (Option<String>, skipped when None)
- `RingBuffer` struct: path + capacity (30); `new(path)`, `default_path()`, `append()`, `read(limit)`, `clear()`
- Atomic writes: entries serialized to `.errors.<pid>.<nanos>.tmp` then renamed via `fs::rename` (POSIX atomic within same filesystem)
- Rotation: load existing entries, push new, drain oldest if len > capacity
- `read(Some(n))`: `saturating_sub` to get start index, slice last N
- `default_path()`: `dirs::data_local_dir()` → `~/.local/share/chronicle/errors.jsonl`
- Parent directory auto-created via `fs::create_dir_all` on append/clear
- serde_json errors converted to `std::io::Error` (via `IoError` variant in `ChronicleError`)
- Updated `src/errors/mod.rs`: added `pub mod ring_buffer;`
- 16 unit tests: read empty, append+read, order preserved, rotation at 31/100 entries, read limits (n, oversized, zero), clear, clear nonexistent, all 7 categories, file+detail round-trip, absent optional fields omitted in JSON, severity lowercase, default_path suffix, parent dir creation
- Files changed: `src/errors/ring_buffer.rs` (new), `src/errors/mod.rs`
- 169 tests total; all passing; cargo fmt, clippy -D warnings, cargo deny all pass
- **Learnings for future iterations:**
  - Atomic write via temp+rename is the standard POSIX pattern; temp file must be on same filesystem (same parent dir) for rename to be atomic — always use `parent.join(...)` not `/tmp/`
  - `serde_json::Error` does not impl `From<serde_json::Error>` for `std::io::Error` directly; wrap with `std::io::Error::new(InvalidData, je.to_string())` then `?` converts via `ChronicleError::IoError`
  - `skip_serializing_if = "Option::is_none"` on optional fields keeps JSON lean; deserialization with absent fields defaults to None automatically
  - `dirs::data_local_dir()` on macOS returns `~/Library/Application Support`; on Linux returns `~/.local/share` — the XDG-compliant path per spec §11.1
  - `ConflictReporter` from US-007 used `NullReporter` as placeholder; US-008 `RingBuffer` is the real impl to wire in when US-015/US-017/US-018 connect the merge pipeline to the ring buffer
---

## 2026-03-29 - US-009
Session: iter-8cd84475 / loop-26ce4e61 iteration 9
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented file change scanner (§14.1) in `src/scan/mod.rs`
- `StateCache`: `load(path)` (returns empty when file missing, propagates other I/O), `save(path)` (atomic via temp+rename), `default_path()` XDG path
- `FileState`: `local_mtime`, `local_size`, `last_synced_size`, `local_path` — full serde round-trip
- `scan_dir(dir, cache, follow_symlinks)` → `Result<Vec<ScanEntry>, ScanError>`
  - Symlink policy: `symlink_metadata(dir)` to detect top-level dir symlink; `follow_symlinks=false` → `tracing::error!` + `Err(SymlinkRefused)`; `follow_symlinks=true` → `tracing::warn!` + proceed
  - `collect_jsonl` recursive walk skips inner symlinks via `ft.is_symlink()` check
  - `classify` uses absolute path as cache key; returns `New`/`Modified`/`Unchanged`
- `ChangeKind` enum, `ScanEntry` struct, `ScanError` enum (thiserror)
- 14 unit tests: missing file, save/load round-trip, parent dir creation, default_path suffix, first run (all new), new/modified/unchanged file, non-jsonl ignored, empty dir, nested subdirs, symlink refused (unix), symlink followed (unix)
- Files changed: `src/scan/mod.rs`
- 183 tests total; all passing; cargo fmt, clippy -D warnings, cargo deny all pass
- **Learnings for future iterations:**
  - `std::io::Error::other(e)` is the idiomatic form; `clippy::io_other_error` fires on `Error::new(ErrorKind::Other, e)`
  - `DateTime::<Utc>::from(system_time)` for `SystemTime → DateTime<Utc>`; uses the `From<SystemTime>` impl in chrono
  - `Utc.timestamp_opt(secs, nsecs)` requires `use chrono::TimeZone;` — import inside `mod tests` block not at file top-level
  - `DirEntry::file_type()` uses lstat (no symlink follow); `ft.is_symlink()` correctly identifies symlinked children inside a directory
  - State cache key: use absolute path string in unit tests; real integration (US-017) will pass canonical repo-relative keys
---

## 2026-03-29 - US-010
Session: iter-7c806c57 / loop-26ce4e61 iteration 10
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `RepoManager` in `src/git/mod.rs` — git repo management via git2
- `init_or_open(path, remote_url)`: opens existing repo or initializes new one;
  uses `ErrorCode::NotFound` guard on `Repository::open` to distinguish "not a repo yet"
  from other failures (corrupt repo, permissions, etc.); parent dirs auto-created
- `ensure_working_tree()`: creates pi/sessions/ + .gitkeep, claude/projects/ + .gitkeep,
  and .chronicle/ directory; fully idempotent
- `ensure_manifest()` / `read_manifest()` / `write_manifest()`: .chronicle/manifest.json
  management; read returns default Manifest (version=1, empty machines) when file absent;
  ensure_manifest is a no-op when file exists; write uses serde_json::to_string_pretty
- `set_remote(name, url)`: adds origin on first init; updates URL (remote_set_url) on reopen;
  empty/None URL skipped so origin is only set when explicitly configured
- `GitError` (thiserror): Git2, Io { path, source }, Manifest variants
- `Manifest` / `MachineEntry` serde structs per §6.3 spec schema;
  last_sync: Option<DateTime<Utc>> with skip_serializing_if=Option::is_none
- Files changed: `src/git/mod.rs`
- 16 new unit tests; 199 total; all pass
- **Learnings for future iterations:**
  - `git2::Repository::open` returns `ErrorCode::NotFound` for both "path doesn't exist"
    and "path exists but isn't a git repo" — matching this variant is sufficient to determine
    that initialization is required; all other errors indicate genuine problems
  - `Repository::init` on an already-initialized repo is idempotent in libgit2, but
    the explicit open-then-init pattern is clearer and avoids any potential config reset
  - `remote_set_url` is the correct method to update an existing remote URL (not `remote_delete`
    + `remote`); `find_remote` returns NotFound for a non-existent remote name
  - US-011 (fetch/push) and US-012 (staging/commits) will add methods to RepoManager —
    they need access to `self.repo` (git2::Repository) and `self.repo_path`
  - `git2::Repository` does not implement `Debug`, so `RepoManager` cannot `#[derive(Debug)]`
---

## Codebase Patterns
- Doctor check functions go in `src/doctor/mod.rs` (extracted from cli/mod.rs because check logic exceeds ~300 lines).
- All doctor check functions accept injected paths/state to enable unit testing without real FS/network.
  - Pass a closure `impl Fn(&str) -> Result<(), String>` for network checks (remote reachability).
  - Pass `&[PathBuf]` for SSH key paths, `&[String]` for crontab lines, `&Path` for lock file.
- `CheckState` / `CheckResult` constructors (`pass`, `warn`, `error`, `skipped`) keep each call site concise.
- Use `#[must_use]` on all public check functions that return `Vec<CheckResult>`.
- The pre-existing flaky test `push_merges_new_entries_with_existing_repo_content` fails under parallel
  test execution (advisory lock contention). Run `cargo test -- --test-threads=1` for a reliable full run.
- `cargo deny check` emits warnings for unused license allowances and duplicate `getrandom` versions;
  these are pre-existing and not errors.
---

## PRD Run Context
PRD Branch: ralph/doctor-command
Started: 2026-04-03T04:13:24.393Z
---

## 2026-04-03 — US-001
Session: iter-b9866bc2
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Created `src/doctor/mod.rs` with:
  - `CheckState` enum (Pass, Warn, Error, Skipped)
  - `CheckResult` struct with key/state/detail/hint fields and concise constructors
  - `check_config(config_path)` → 3 results: config.file, config.toml, config.remote
  - `check_git(repo_path, remote_url, ssh_key_paths, check_remote)` → 3 results:
    git.repo, git.remote (skipped when no remote), git.ssh_key (skipped for HTTPS)
  - `check_agents(pi_enabled, pi_dir, claude_enabled, claude_dir)` → 2 results per agent
  - `check_scheduler(crontab_lines, lock_path, lock_timeout_secs)` → 2 results
  - `default_check_remote()` and `default_ssh_key_paths()` for production use
  - `parse_remote_host_port()` / `split_host_port()` for URL parsing
  - 23 unit tests covering all pass/warn/error/skipped branches
- Added `pub mod doctor;` to `src/lib.rs`
- Files changed: src/doctor/mod.rs (new), src/lib.rs
- **Learnings for future iterations:**
  - Extracted to `src/doctor/mod.rs` immediately because check logic + tests exceeded 300 lines.
  - The `check_git` closure pattern (`impl Fn(&str) -> Result<(), String>`) is the right way
    to inject network behaviour; use it for any future command with external I/O.
  - `is_pid_alive` is now duplicated in cli/mod.rs and doctor/mod.rs. If it grows, extract
    to a shared internal module (e.g. `src/util.rs`).
  - Parallel test execution causes flaky lock-contention failures in push tests — known issue.
---

## 2026-04-03 — US-002
Session: iter-230fbbcf
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Wired `chronicle doctor` into the CLI (src/main.rs + src/cli/mod.rs)
- Added `DoctorArgs { porcelain: bool, no_color: bool }` struct
- Added `handle_doctor`, `doctor_impl`, `doctor_write` as layered entry points
- Added `run_doctor_checks` — collects live check results (FS, network, crontab)
- Added `format_doctor_results` — pure formatter accepting `DoctorCheckResults`
  and any `io::Write` writer; tested directly from unit tests
- Added `emit_doctor_section` — section header + per-check lines + blank line
- Extended `StatusFormatter<W>` with `section_header`, `hint_line`,
  `skipped_check`, `blank_line`, `raw_line` methods
- Exit codes: 0 all-pass, 1 warnings-only, 2 any-error (via std::process::exit)
- Color logic: NO_COLOR env / --no-color flag / --porcelain all suppress ANSI
- Files changed: src/main.rs, src/cli/mod.rs, .ralphi/prd.json, .ralphi/progress.txt
- **Learnings for future iterations:**
  - `format_doctor_results` takes `DoctorCheckResults` by value (all Vecs);
    tests inject fake `DoctorCheckResults` directly — no real FS or crontab needed.
  - `StatusFormatter.writer` is private; added `raw_line` for unconditional writes.
  - `blank_line` / `section_header` / `hint_line` check `self.porcelain` so
    US-003 porcelain mode can set `porcelain=true` on the formatter to suppress
    all human-readable lines and emit its own via `raw_line`.
  - The summary uses `·` (U+00B7 middle dot) and `→` (U+2192) for hint prefix —
    confirm these render on target terminals before changing.
---

## 2026-04-03 — US-003
Session: iter-780516bf
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `--porcelain` mode in `format_doctor_results`:
  - Emits `check.<key>=<state>[:<detail>]` for every CheckResult in section order
  - Values: `ok[:<detail>]`, `warning:<detail>`, `error:<detail>`, `skipped[:<reason>]`
  - Emits `summary.errors=N`, `summary.warnings=N`, `summary.passed=N` as last three lines
  - No ANSI codes; `--porcelain` implies `--no-color` (enforced in handle_doctor)
- Added `porcelain_check_value()` private helper for state→porcelain-string mapping
- Made `DoctorCheckResults` pub with pub fields (enables integration test injection)
- Made `format_doctor_results` pub (enables integration test direct calls)
- Updated DoctorArgs.porcelain doc comment (removed deferred TODO)
- Added 3 integration tests in `tests/integration.rs`:
  - `doctor_porcelain_happy_path_exit_0`: injects all-pass `DoctorCheckResults`
    via `format_doctor_results`; verifies exit 0, all check lines ok/skipped, no ANSI
  - `doctor_porcelain_config_file_missing_exit_2`: calls `doctor_write` with
    non-existent config path; verifies exit 2, `check.config.file=error:<detail>`
  - `doctor_porcelain_agent_sessions_dir_missing`: calls `doctor_write` with
    absent pi sessions dir; verifies exit >= 1, `check.agents.pi=error:<detail>`
- All 14 integration tests pass (`cargo test -- --test-threads=1`)
- Files changed: src/cli/mod.rs, tests/integration.rs, .ralphi/prd.json, .ralphi/progress.txt
- **Learnings for future iterations:**
  - `format_doctor_results` must be pub for integration tests to inject results;
    the pattern (inject `DoctorCheckResults` directly) avoids all FS/network deps.
  - For `doctor_write` integration tests (real FS): omit remote_url in config to
    get `git.remote=Skipped` rather than a slow network timeout.
  - `porcelain_check_value` is the single source of truth for the format;
    keeping it separate makes it easy to audit against spec.
  - No new dependencies required — all US-003 features are pure Rust std.
---
